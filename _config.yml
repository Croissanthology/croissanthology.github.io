# Site settings
title: Croissanthology
description: "Writing about rationality, mundane utility, and our gloriously apollonian expansion into the lightcone. Essays on AI risk, cognitive biases, death, and human progress."
url: "https://croissanthology.github.io"
baseurl: "" 

# Build settings
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
permalink: /:title/

# Collections for different sections
collections:
  essays:
    output: true
    permalink: /essays/:title/
  popular:
    output: true
    permalink: /popular/:title/

# Plugins
plugins:
  - jekyll-feed
  - jekyll-minifier
  - jekyll-redirect-from

# Feed settings
feed:
  path: feed.xml
  posts_limit: 1000  # Include all posts for maximum discoverability

# Theme settings
theme: null  # Using custom theme
sass:
  style: compressed

# Default layouts
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "default"
  -
    scope:
      path: ""
      type: "essays"
    values:
      layout: "default"
  -
    scope:
      path: ""
      type: "popular"
    values:
      layout: "default"

# Exclude files from processing
exclude:
  - README.md
  - LICENSE
  - .gitignore

